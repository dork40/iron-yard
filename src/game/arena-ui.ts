import type { ArenaConfig, ArenaPhase } from "./arena-state";
import { weapons, type WeaponId } from "./weapons";

const weaponIds: WeaponId[] = ["frontier-rifle", "modern-rifle", "pistol"];

function weaponButton(id: WeaponId, action: "buy" | "equip") {
  const weapon = weapons[id];
  const attribute = action === "buy" ? "data-arena-loadout" : "data-arena-equip";
  return `<button class="arena-shop-item" ${attribute}="${id}" data-weapon="${id}"><span><b>${weapon.name}</b><small>${weapon.price ? `$${weapon.price}` : "STANDARD ISSUE"}</small></span><em data-weapon-state="${id}">AVAILABLE</em></button>`;
}

export function arenaView(online: boolean, config: ArenaConfig) {
  return `<section class="arena-game"><div class="arena-heading"><p class="eyebrow">IRON YARD // TACTICAL PRACTICE</p><h1>IRON YARD</h1><p>Original procedural industrial yard. Practice tactical rounds, run drills, or open a lightweight private 1v1.</p></div><div class="arena-menu" id="arena-menu"><div><p class="eyebrow">OFFLINE BOT MATCH</p><h2>SET THE YARD</h2><label>BOT DISCIPLINE<select id="arena-difficulty"><option value="rookie" ${config.difficulty === "rookie" ? "selected" : ""}>ROOKIE</option><option value="standard" ${config.difficulty === "standard" ? "selected" : ""}>STANDARD</option><option value="veteran" ${config.difficulty === "veteran" ? "selected" : ""}>VETERAN</option></select></label><label>OPPOSITION<select id="arena-bot-count"><option value="1" ${config.botCount === 1 ? "selected" : ""}>1 BOT</option><option value="2" ${config.botCount === 2 ? "selected" : ""}>2 BOTS</option><option value="3" ${config.botCount === 3 ? "selected" : ""}>3 BOTS</option></select></label><div class="arena-menu-actions"><button id="arena-bot" class="primary">PLAY BOT MATCH</button><button id="arena-drill" class="outline">RECOIL DRILL</button></div></div><div><p class="eyebrow">ONLINE 1V1</p><h2>PRIVATE YARD</h2>${online ? `<button id="arena-create" class="outline">CREATE 1V1</button><label>ROOM <input id="arena-room-code" maxlength="6" /></label><button id="arena-join" class="outline">JOIN ROOM</button>` : `<p class="arena-muted">Set VITE_ARENA_SERVER_URL to enable server-validated private rooms.</p>`}<button id="arena-return" class="text-button">RETURN TO SITE</button></div></div><div class="arena-frame" aria-label="Iron Yard game view"><div id="arena-canvas"></div><div class="arena-round-hud"><span id="arena-rival"></span><b id="arena-phase"></b><span id="arena-score"></span></div><div class="arena-status"><span id="arena-health"></span><span id="arena-ammo"></span><span id="arena-cash"></span></div><p class="arena-message" id="arena-message" aria-live="polite">SELECT A MATCH TO ENTER THE YARD.</p><div class="arena-reticle" id="arena-crosshair" aria-hidden="true"><i></i><b></b><em></em></div><p class="arena-feedback" id="arena-feedback" aria-live="polite"></p><button class="arena-lock" id="arena-lock" hidden>CLICK YARD TO DEPLOY</button><div class="arena-pause" id="arena-pause" hidden><b>PAUSED</b><span>SELECT A HUD ACTION OR CLICK THE YARD TO RESUME.</span></div><nav class="arena-action-bar" aria-label="Arena actions"><button data-arena-panel="buy">BUY <kbd>B</kbd></button><button data-arena-panel="loadout">LOADOUT</button><button data-arena-panel="settings">SETTINGS</button><button data-arena-panel="pause">PAUSE / RETURN</button></nav><section class="arena-panel" id="arena-buy-panel" hidden aria-label="Buy menu"><header><p class="eyebrow">ARMORY</p><h2>BUY LOADOUT</h2><button class="arena-panel-close" data-arena-close aria-label="Close buy menu">CLOSE</button></header><div class="arena-buy-readout"><b id="arena-buy-cash">CASH $0</b><span id="arena-buy-phase">BUY CLOSED</span><span id="arena-buy-zone">START ZONE REQUIRED</span></div><p class="arena-panel-note" id="arena-buy-requirement">BUYING IS OPEN ONLY DURING THE BUY PHASE IN THE START ZONE.</p><div class="arena-shop-list">${weaponIds.map(id => weaponButton(id, "buy")).join("")}</div></section><section class="arena-panel" id="arena-loadout-panel" hidden aria-label="Loadout menu"><header><p class="eyebrow">CARRIED WEAPONS</p><h2>LOADOUT</h2><button class="arena-panel-close" data-arena-close aria-label="Close loadout menu">CLOSE</button></header><p class="arena-panel-note">Owned weapons can be equipped at any time. Buy new weapons from the armory during the buy phase at the start zone.</p><div class="arena-shop-list">${weaponIds.map(id => weaponButton(id, "equip")).join("")}</div></section><section class="arena-panel arena-settings-panel" id="arena-settings-panel" hidden aria-label="Settings"><header><p class="eyebrow">LOCAL CONTROLS</p><h2>SETTINGS</h2><button class="arena-panel-close" data-arena-close aria-label="Close settings">CLOSE</button></header><div class="arena-settings-grid"><label>SENSITIVITY <input id="fps-sensitivity" type="range" min="0.001" max="0.006" step="0.0001" /><output id="fps-sensitivity-value"></output></label><label>GRAPHICS QUALITY <select id="graphics-quality"><option value="low">LOW</option><option value="medium">MEDIUM</option><option value="high">HIGH</option></select></label><label class="arena-check"><input id="graphics-shadows" type="checkbox" /> SHADOWS</label><label>RETICLE COLOR <input id="cross-color" type="color" /></label><label>RETICLE SIZE <input id="cross-size" type="range" min="3" max="24" /></label><label>PIXEL RATIO <input id="graphics-pixel-ratio" type="range" min="0.7" max="2" step="0.1" /></label></div><div class="binding-grid"><button data-bind="forward">FORWARD <b></b></button><button data-bind="back">BACK <b></b></button><button data-bind="left">LEFT <b></b></button><button data-bind="right">RIGHT <b></b></button><button data-bind="reload">RELOAD <b></b></button><button data-bind="jump">JUMP <b></b></button></div><p id="binding-notice" class="arena-muted">Select a keybind, then press a key.</p><div class="arena-settings-actions"><button id="settings-reset" class="outline">RESET DEFAULT SETTINGS</button><button id="cross-share" class="outline">COPY RETICLE</button><label>IMPORT RETICLE <input id="cross-import" /></label></div></section><section class="arena-panel arena-pause-panel" id="arena-pause-panel" hidden aria-label="Pause menu"><header><p class="eyebrow">YARD PAUSED</p><h2>PAUSE / RETURN</h2><button class="arena-panel-close" data-arena-close aria-label="Resume">RESUME</button></header><p class="arena-panel-note">Gameplay is paused locally while this panel is open. Return to the site ends this arena session.</p><div class="arena-pause-actions"><button data-arena-close class="primary">RESUME YARD</button><button id="arena-return-panel" class="outline">RETURN TO SITE</button></div></section></div></section>`;
}

export function updateArenaHud(elements: { health: HTMLElement; ammo: HTMLElement; rival: HTMLElement; phase: HTMLElement; cash: HTMLElement; score: HTMLElement }, value: { health: number; ammo: string; rival: string; phase: ArenaPhase; phaseLabel: string; cash: number; eliminations: number; deaths: number }) {
  elements.health.textContent = `HEALTH ${Math.ceil(value.health)}`;
  elements.ammo.textContent = value.ammo;
  elements.rival.textContent = value.rival;
  elements.phase.textContent = value.phaseLabel;
  elements.phase.dataset.phase = value.phase;
  elements.cash.textContent = `$${value.cash}`;
  elements.score.textContent = `${value.eliminations} ELIMS · ${value.deaths} DEATHS`;
}
