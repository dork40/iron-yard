import * as THREE from "three";
import type { GraphicsSettings } from "./settings";
export function applyGraphicsSettings(renderer: THREE.WebGLRenderer, graphics: GraphicsSettings) { renderer.setPixelRatio(Math.min(devicePixelRatio, graphics.pixelRatio)); renderer.shadowMap.enabled = graphics.shadows; renderer.shadowMap.type = THREE.PCFSoftShadowMap; }
export function createRenderer(host: HTMLElement, graphics: GraphicsSettings) { const renderer = new THREE.WebGLRenderer({ antialias: true, powerPreference: "high-performance" }); applyGraphicsSettings(renderer, graphics); renderer.outputColorSpace = THREE.SRGBColorSpace; renderer.toneMapping = THREE.ACESFilmicToneMapping; renderer.toneMappingExposure = 1.05; host.replaceChildren(renderer.domElement); return renderer; }
export function createWeapon(camera: THREE.Camera) { const group = new THREE.Group(); const steel = new THREE.MeshStandardMaterial({ color: "#252b2c", metalness: .8, roughness: .28 }); const wood = new THREE.MeshStandardMaterial({ color: "#5b3726", roughness: .76 }); const receiver = new THREE.Mesh(new THREE.BoxGeometry(.17,.16,.65),steel); receiver.position.set(.3,-.27,-.65); const barrel = new THREE.Mesh(new THREE.CylinderGeometry(.035,.045,.68,10),steel); barrel.rotation.x=Math.PI/2; barrel.position.set(.3,-.23,-1.15); const grip = new THREE.Mesh(new THREE.BoxGeometry(.13,.28,.16),wood); grip.position.set(.3,-.43,-.47); grip.rotation.x=-.2; group.add(receiver,barrel,grip); camera.add(group); return group; }
export function createFighter(color = "#4e6670") { const group = new THREE.Group(); const cloth = new THREE.MeshStandardMaterial({ color, roughness:.8 }); const skin = new THREE.MeshStandardMaterial({ color:"#8e614a",roughness:.9 }); const body = new THREE.Mesh(new THREE.CapsuleGeometry(.34,.8,5,8),cloth); body.position.y=.85; const head=new THREE.Mesh(new THREE.SphereGeometry(.23,12,8),skin); head.position.y=1.58; group.add(body,head); group.traverse(item => { const mesh=item as THREE.Mesh; mesh.castShadow=true; }); return group; }
